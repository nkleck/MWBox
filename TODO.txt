TO DO

 start working on tools.sh 
  - so a bunch of github repo's, not tying them to this repo, but will dl then upon install
    - this means modifying the script with PATH so they dl and install the ones that need setup
    - need to build in version checking on tool scripts, if outdated, remove old version, install new

Things being needed done:
- fix yara-python portion of script
- fix unpacking clamav script
- add some tools to tool.sh script
- add  malware sandbox stuff
- install WINE?
- create a cleanup scrip that is last ran in bootstrap
- cleans up file locations
- moves setup scripts to config folder, hide config folder
