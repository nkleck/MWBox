TO DO

 start working on tools.sh 
  - so a bunch of github repo's, not tying them to this repo, but will dl then upon install
    - this means modifying the script with PATH so they dl and install the ones that need setup
    - need to build in version checking on tool scripts, if outdated, remove old version, install new

Things being needed done:
- fix yara-python portion of script
- fix unpacking clamav script
- add some tools to tool.sh script
- add  malware sandbox stuff
- install WINE?
- create a cleanup scrip that is last ran in bootstrap
- cleans up file locations
- moves setup scripts to config folder, hide config folder

- apt-get install whois
- apt-get install dnsutils

DNSMap
$ wgie7 http://dnsmap.googlecode.com/files/dnsmap-0.30.tar.gz
$ tar -xvzf dnsmap-0.30.tar.gz
$ cd dnsmap-0.30
$ make
$ sudo make install

add whois -h asn.shadowserver.org instructions to toolbox readme

netcat
apt-get install netcat

have a text file in following format
begin origin
ip
ip
ip
end

nc asn.shadowserver.org 43 < ip.txt > asn.txt

IDEA
maybe write a python script that checks AAProject and Spamcop blacklists and returns the output to a file or screen

mayabe this tool later on, not now
$ wget -N -q http://geolite.maxmind.com/download/geoip/database/GeoLiteCity.dat.gz
$ gzip -d GeoLiteCity.dat.gz 
$ ls -alh GeoLiteCity.dat
$ git clone https://github.com/appliedsec/pygeoip.git
$ cd pygeoip/
$ wget http://svn.python.org/projects/sandbox/trunk/setuptools/ez_setup.py
$ python setup.py build
$ sudo python setup.py install

$ python
>>> import pygeoip
>>> gip = pygeoip.GeoIP('GeoLiteCity.dat')
>>> rec = gip.record_by_name('yahoo.com')
>>> for key,val in rec.items():
... 	print "%s: %s" % (key,val)

- automate that with a python script that only requires user imput the rec
- would need to write a cron job to auto dl the newest db when they are available (usually monthly)
